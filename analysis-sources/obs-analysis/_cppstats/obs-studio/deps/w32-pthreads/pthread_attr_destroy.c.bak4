



































#include "pthread.h"
#include "implement.h"


int
pthread_attr_destroy (pthread_attr_t * attr)






















{
if (ptw32_is_attr (attr) != 0)
{
return EINVAL;
}




(*attr)->valid = 0;
free (*attr);
*attr = NULL;

return 0;
}
