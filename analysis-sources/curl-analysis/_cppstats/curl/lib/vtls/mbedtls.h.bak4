#if !defined(HEADER_CURL_MBEDTLS_H)
#define HEADER_CURL_MBEDTLS_H






















#include "curl_setup.h"

#if defined(USE_MBEDTLS)

extern const struct Curl_ssl Curl_ssl_mbedtls;

#endif 
#endif 
