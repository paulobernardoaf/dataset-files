



















#if defined(HAVE_CONFIG_H)
#include <config.h>
#endif

#include <stdlib.h>

#if !defined(__ANDROID__)
double atof (const char *str)
{
return strtod (str, NULL);
}
#endif
